query getAllPosts($off: Int! = 0, $size: Int! = 10)
{
    getBoard(id: 1){
        posts(offset: $off,size: $size)
        {
            id
            title
            content
            member{
                name
                picture
            }
            createdAt
        }
    }
}